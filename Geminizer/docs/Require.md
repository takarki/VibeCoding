Chrome拡張プラグインを作りたい。内容としては ページを閲覧中にプラグインのボタンを押すと URLを読み出して特定のプロンプトに応じて要約を作成してそのGeminiを開いてコンテンツを作成するというものである。

例えばYouTubeのケースでは、 
サンプルとしてはこのような形で実現したい。 

https://gemini.google.com/share/1af1b7f7f63f

このページでは単なる要約になっているが、 時間のポイントなどにはリンクが埋め込まれておりクリックした場合にはその箇所を見れるようにしたい 

YouTube以外の用途としては例えばそのページを 分析した上で HTMLのインフォグラフィックにまとめる用途やNanoBanana Proで解説用の図解を作る、ポッドキャストを作るなどそういったことを実現したい。

上記のような例のプロンプトをあらかじめ設定しておいてほしいのと、設定できるプロンプトは自由に無条件に増やせるようなものを作りたい。

最初はGeminiだが、その後はJetGPTやクロード、ブロックなども利用できるようにしたい。またはモニカなども範囲に入れたい。

まずこれを要求仕様としてドキュメントにまとめその次に要件定義を実施するフェーズまで進めてください

不明な点や 曖昧な点、こうした方がいいような提案は積極的にお願いします。